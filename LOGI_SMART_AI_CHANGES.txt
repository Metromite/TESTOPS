LOGI SMART AI FINAL

- Removed the duplicate LOGI image from the chat header; header now shows only “LOGI Assistant”.
- Kept the large floating LOGI launcher unchanged.
- Added an animated LOGI avatar beside assistant chat messages and an animated typing indicator.
- Added a speech-bubble triangle tail so assistant messages visibly originate from LOGI.
- LOGI now sends conversation history and current screen to a Supabase Edge Function.
- Added cloud LOGI agent `logi-agent` with live DispatchOPS app context and live-table tools.
- With an AI provider configured in AI Settings, LOGI can reason across the app, inspect live records, calculate/compare data, suggest next actions, navigate to screens, and prepare database changes.
- Writes are never silently executed: LOGI shows a confirmation card first. Confirmed writes are executed server-side and recorded in audit_log.
- Added read access to the operational tables used by DispatchOPS, with allowlisted tables and filters.
- Added provider fallback handling through the existing AI Settings fallback chain.
- Provider secrets remain server-side in the Supabase Edge Function; no service-role key is included in the desktop app.
- Existing background, export, storage, connection, import, and main LOGI floating-button work is retained.
