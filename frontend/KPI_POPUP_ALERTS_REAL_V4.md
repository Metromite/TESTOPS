# KPI Popup + Alerts Real Fix V4

- Restored the exact pre-UI-update KPI tile click implementation using GlassCard onClick.
- Restored the exact pre-UI-update Modal implementation using GlassModal/Radix.
- Restored the exact pre-UI-update getHomeDetailRows implementation and semantics.
- Valid Invoices and Total Boxes now use the legacy invoice-detail popup path, matching the old working version.
- Today's Schedule is one glass surface; alerts/KPIs/tools are flat internal content with separators only.
- Existing monthly fleet/default persistence changes are retained.
