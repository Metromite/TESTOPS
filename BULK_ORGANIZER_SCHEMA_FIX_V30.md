# Bulk Organizer v30 fixes

- The `bulk_organizer_plans` table has been applied to the primary DispatchOPS Supabase project.
- The migration remains included in `supabase/migrations/0045_bulk_organizer.sql` and `supabase/RUN_ALL_MIGRATIONS.sql`.
- Liquid Glass theme CSS no longer overrides runtime `body.custom-bg` wallpapers, so Appearance-configured light/dark backgrounds remain visible.
